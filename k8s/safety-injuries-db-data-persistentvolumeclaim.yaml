apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: safety-injuries-db-data
  namespace: uchaly
  labels:
    io.kompose.service: safety-injuries-db-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi