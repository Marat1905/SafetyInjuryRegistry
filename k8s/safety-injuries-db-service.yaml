apiVersion: v1
kind: Service
metadata:
  name: safety-injuries-db
  namespace: uchaly
  labels:
    io.kompose.service: safety-injuries-db
spec:
  type: NodePort
  ports:
    - name: "tcp-db"
      nodePort: 30152
      port: 5432
      targetPort: 5432
  selector:
    io.kompose.service: safety-injuries-db